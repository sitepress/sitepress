// This is the main application JavaScript file.
//
// Propshaft does not use directives or asset compilation.
// Files are served as-is with digested filenames for cache busting.
//
// Use import maps or a JavaScript bundler (like esbuild, rollup, or webpack)
// for managing dependencies and modules.